[project]
name = "ethbootstrap-util"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "solc-select>=1.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
